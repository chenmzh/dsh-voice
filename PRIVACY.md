# 隐私与仓库边界

本仓库从干净的源码副本创建，没有导入开发机器的 Git 历史、录音、音色参考文件、私人转写、聊天记录、截图、日志、备份、DSH 设置或凭据。模型权重和第三方引擎仓库也不随代码上传。

`.gitignore` 默认排除音视频、截图、模型、数据库、转写文本、环境变量文件及本地运行目录。`tools/check-privacy.mjs` 使用源码文件白名单，并检查绝对主目录路径、常见令牌、私钥和内嵌长音频。提交前运行全目录检查及暂存区检查；自动检查不能识别所有形式的私人文本，应同时检查 diff。

## 运行时的数据流

- 本地识别：浏览器把麦克风音频发送给你部署的 DSH 宿主，由本地 ASR 引擎处理；“本地”指 DSH 服务器，不一定是打开网页的电脑。
- 本地朗读：回复或选中的正文发送给 DSH 宿主的 TTS 引擎，合成音频返回浏览器播放。
- 克隆音色：录音及参考转写保存在 DSH 宿主的参考目录，默认 `$DSH_HOME/voice-refs`（未设置时为 `~/.dsh/voice-refs`），不会写入 Git 仓库。
- 如果显式选用浏览器 Web Speech，音频可能交由浏览器厂商的识别服务处理；需要完全本地处理时使用 `native` 后端。本插件默认使用 `native`。
- 下载依赖和模型时需要访问包管理器及模型来源。推理 worker 默认启用 Hugging Face/Transformers 离线模式，不会自动下载缺少的模型。

不要把参考音色、录音、设置文件或令牌放进本仓库，也不要使用 `git add -f` 绕过忽略规则。私有仓库本身也不是上传私人音色的理由。
